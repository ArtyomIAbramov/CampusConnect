<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- TODO: change colors when UI will be ready-->
    <style name="Theme.CampusConnect" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_beige</item>
        <item name="colorPrimaryVariant">@color/primary_beige</item>
        <item name="colorOnPrimary">@color/primary_beige</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_beige</item>
        <item name="colorSecondaryVariant">@color/primary_beige</item>
        <item name="colorOnSecondary">@color/primary_beige</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ThemeOverlay.CampusConnect.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_900</item>
        <item name="fullscreenTextColor">@color/light_blue_A400</item>
    </style>
</resources>